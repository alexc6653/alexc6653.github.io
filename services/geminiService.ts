
// Dieser Service wird nicht mehr aktiv genutzt, da die Anwendung auf manuellen Upload umgestellt wurde.
export const generateMovieMetadata = async (title: string): Promise<null> => {
  return null;
};
